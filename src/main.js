new p5((p) => {
  p.preload = () => {
    // Preload assets if needed
  };

  p.setup = () => {
    //
  };

  p.draw = () => {
    //
  };

  p.keyPressed = (e) => {
    //
  };

  p.keyReleased = (e) => {
    //
  };
});
